name: Deploy Templates on Subscription level

on:
  push:
    branches:
      - development
env:
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURESUBSCRIPTIONID }}   # set this to your Azure Subscription Id
  AZURE_RESOURCE_GROUP: GitHubTest-rg   # set this to your target resource group
  AZURE_REGION: eastus2

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2

      - name: Bicep Build-1
        uses: aliencube/bicep-build-actions@v0.1
        with:
          files: sandbox/main.bicep

      #create artifact tar
      - name: 'Tar files'
        run: tar -cvf bicep-artifacts.tar ./

      #upload artifacts
      - name: Upload Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: bicep-artifacts
          path: bicep-artifacts.tar

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      # download and extract artifacts
      - uses: actions/download-artifact@v2
        with:
          name: bicep-artifacts

      # extract files
      - name: 'Extract Files'
        run: tar -xvf bicep-artifacts.tar

      # Login to Azure
      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_SUB_CRED }}

      - name: Deploy ARM Template
        uses: azure/arm-deploy@v1
        with:
          scope: subscription
          subscriptionId: ${{ env.AZURE_SUBSCRIPTION_ID }}
          resourceGroupName: ${{ env.AZURE_RESOURCE_GROUP }}
          region: ${{ env.AZURE_REGION }}
          template: ./sandbox/main.json
